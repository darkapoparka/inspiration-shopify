# Optional. Without this value the public demo persists in the browser.
# With a Neon PostgreSQL connection string, every visitor receives an isolated
# server-persisted demo workspace via an HTTP-only session cookie.
DATABASE_URL=
